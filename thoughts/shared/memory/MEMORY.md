- [codex 0029 gotchas](codex-0029-gotchas.md) — PROJECT 2026-07-14 **▶ P2 ENTITY PAGES BUILT — S6 `b174b15` (P1.6: npc-only, stats schemaVersion 2, `_index` rename; corpus 46,192) · S1 `031a7fb` (total 18-kind renderer + 6 goldens) · S2 `72f224e` (scaffold + traversal-guarded reader + `@legacy`/non-ASCII routes) · S3 `c9d1d3b` (listings + Popover; A–G met, 909 member tests). ▶ NEXT: acceptance H stakeholder page review (M7 links-not-inlined + M11 statblock-twice are EXPECTED) → spec P3. THE P2 finds: serverFn co-located exports leak into the client bundle (route-unreachable pure-file split + bundle byte-search); import.meta.dirname breaks under dist (marker-walk); two plausible-but-wrong hardcoded game-data finds caught only by corpus cross-check (inlineAction costs, per-request Zod vs D29-23); blockquote corpus-extinct.**
  <br>_(P1, superseded detail:)_ P1 INGEST BUILT — all 4 slices committed same-day (S1 `108571d` fetchers+snapshots · S2 `40b2447` schema+Foundry parser · S3 `8465625` AoN parser+licenseMap · S4 `8d66293` join+emit+fixture+`just codex-refresh`), 503 tests, determinism proven; corpus = 50,952 entities / 97 categories / 656 MB (spec guessed 100–200 — P5 sizing input). ▶ P1 EXIT GATE = stakeholder review of `data/corpus/report.md` — esp. the 9 STOP categories <50% joined (domain 0% "X Domain" vs "X"; armor/weapon/shield tiered variants; class-feature/creature-ability granularity; creature 57.6% overall though dragons prove 13.8%→98.1%) — re-decide join keys BEFORE P2.** A public-but-NOINDEXED PF2e reference site (`codex.iridi.cc`, port 10374, flat `apps/codex`). THE finds: packs have NO system.slug (file basename IS the slug, 28,636/28,636 port agreement); AoN urls NOT unique (2,269 collision groups); licenseMap zero-unknown (the "(Remastered)"-title override is load-bearing); plain crossrefs can't disambiguate renamed collision ids (only embeds carry provenance); emit-time Zod caught 3 real-corpus bugs; Dockerfile ripple = 13 files not 21; session-limit-killed background agents resume cleanly via SendMessage.
- [portal-player 0028 gotchas](portal-player-0028-gotchas.md) — PROJECT 2026-07-11 **COMPLETE — S1–S3 built + live gate A–H same day**: second static key `portal_player_api_key` on `/mcp` → exactly 5 read-only tools (bridge-status, query-party, query-player ×7 sections, query-item tri-scope, query-rolls public-only cursor-paginated), markdown at the server. **⭐ THE finding: source-vs-live field OWNERSHIP** — live `game.messages` hydrate `rolls[]` into Roll INSTANCES (JSON.parse threw, rows=0 over 2,454 msgs; accept string|object via stringify round-trip); heroPoints SOURCE-only (live tree zeroed by pf2e prep); spell entry DC on NEITHER source nor live entry system → actor-level `system.attributes.spellDC.value`; hp.max live-only; **fixtures are source-shaped ⇒ hydrated-instance bugs are only catchable live**. Deploy: **env-override name = SOPS key UPPER-CASED not the kdl field name** (mismatch crash-looped the first deploy); mint key BEFORE redeploy; module update → restart portal-headless (WS redial ≠ code reload) + `/mcp` reconnect. F passed non-vacuous (7 real `default=0` world items absent, boundary exact; bestiary pack gate proven vs `PLAYER:LIMITED` 67/94). D28-11 amended: unfiltered spells ALWAYS group-summary (real Argyle full render 12,215 > 12k cap). totalMessages=2454. Onboarding+rotation recipes in the file; key transited chat sanctioned, rotation cheap.
- [headless-gm 0027 gotchas](headless-gm-0027-gotchas.md) — PROJECT 2026-07-11 **COMPLETE — all acceptance A–H ✅ incl. the ≥24h soak (2026-07-11: zero in-window ERROR, WARNs all demoted world noise, one clean sub-second self-heal after a Foundry kick, relaunches=0, bridge stable). Live gate 2026-07-08/09: zero-tab read+write as Portal; restart→in-world ~5s; the oscillation OBSERVED live in the empty-setting window then dead; Setup-session politeness; E = bounce ×2, auto-launch ~20s + full self-heal in ~36s no-recreate. ⭐ E exposed THE stale-page bug: a passive classifier can't see server recovery (dead DOM stuck world-down forever) → classify() re-navigates before classifying non-game/non-join pages (`c553290`). Live gotchas: set bridge-user-id BEFORE headless-up; `not-designated` w/ green bridge-status = mis-pasted setting (console shows ids WITH quotes; reads live, no F5); Playwright 1280×720 < Foundry min 1366×768 (`bda23ee` 1600×900); page-console lines demoted to WARN (`6fd8d5e`) so world noise can't page Class A. Portal user id `xlC6LfQ7godJVVFf`** — supervised headless-Chromium GM session (`apps/portal/headless`, 10373) so portal works with ZERO human tabs. **THE findings: two live GM sessions ping-pong the bridge forever (replace-adopt + 1s backoff reset) → world-scoped `bridge-user-id` strict designated dialer, no fallback, re-checked in dispatchQuery (`not-designated`); Foundry NEVER auto-rejoins (kick/shutdown/≥5s outage → /join, no credential resubmit) — the supervisor loop IS the product; live `options.json` `world:null` (restart strands at /setup; FOUNDRY_WORLD env dead under CONTAINER_PRESERVE_CONFIG) → one-line file edit at S4; `core.noCanvas` safe (no portal handler touches canvas); classify in-world POSITIVELY (`/game`), unknown→world-down; world-down ≠ unhealthy; `page.on(console)`→telemetry is the only visibility into in-page module warns; server-rollback vs installed 0.3.0 module fails handshake as `bad-key` (looks like key leak).** Build: FoundryUsersCollection (not FoundryWorldCollection), vi.hoisted function-mock for `new`, no env override for plain config fields (bind-mount config.kdl in container tests), `shm_size: 1gb`, Pydantic mirror load-bearing (extra=forbid). ⚠️ incidental: host Caddy admin API :2019 unauthenticated w/ plaintext Cloudflare token (surfaced, not astra).
- [portal-authoring 0026 gotchas](portal-authoring-0026-gotchas.md) — PROJECT 2026-07-08 **COMPLETE — built+deployed+live-accepted same session**: 8 new tools (18 total) — create-actor/item/light/macro, apply-condition, update-document, delete-document (stamp-gated D-4), execute-macro (own module setting) — D-1 hybrid supersedes 0023 D5; D-6 `flags[astra-portal]` stamp on every create (0023 retrofit). **Live-proven:** hand-authored NPC zero-warnings, aura two-item pattern, baseUuid clone+patch spell→entry link, embedded-uuid light CRUD, persistent-damage NON-dialog path (source-verified — bare increaseCondition pops a GM dialog), PC HP edit + derived-path deny-list firing, execute-macro returns values, full `portal.audit.*` trail. **THE gotchas: a session's MCP tool list is a SNAPSHOT (redeploy w/ new tools → user must `/mcp` reconnect; interim = scratchpad MCP client + `sops exec-env`, never key-to-file); `just portal-module-install` EACCES (module dir root:999 from Foundry's own install → docker-as-root cp); classifier gates (just up needs "deploy it"; execute-macro auto-blocked, hand to user; deletes of non-session targets need the user to NAME them); module version discipline started (0.2.0).** Residue: unstamped-delete refusal + macro-setting-off toggle unit-only; Goblin Warrior debris pre-stamp (GM hand-delete); Ozzie left at 80 HP.
- [mouthpiece 0024 gotchas](mouthpiece-0024-gotchas.md) — PROJECT 2026-07-07 **BUILT + DEPLOYED + LIVE**: script rework — distill/beats → clean+enrich (windowed OOC filter → compact kept_ranges + floor; enrich → synopsis+wikiRefs), Pass A debates the full cleaned transcript + being.kdl roster + narrative-mechanics rule; threads/mega/one-shot DELETED; digest.json+synopsis contract held (zero downstream edits); 2026-6-29 re-rendered+published ("The Canary in the Undercroft"); **THE finding: the Whisper CONFIDENCE heuristic misses the you/thank-you hallucination family (1,041 survived it live) — the TEXT prong (HALLUCINATION_TEXT_RE in astra_llm, shared scribe+mouthpiece) is what kills it; scribe two-prong gate shipped, 7-6 re-transcribed clean (5,821→4,777) + episode re-rendered live ("The Heart in the Basement")**
- [session 2026-7-6 processing fixes](session-processing-2026-7-6-fixes.md) — PROJECT 2026-07-07 **DONE + LIVE** — two publish bugs from processing the 2026-7-6 session, both prior-infra-change fallout: (1) `session_transcripts` EACCES writing `shibboleth.json` (package-root write, NOT a bind-mount → root-owned image layer → uid-1000 can't write after the run-as-1000 cutover) → chown `/repo/apps/linguist` in the Dagster Dockerfile; (2) `mouthpiece-publish`/`-seed` still referenced the deleted faerrin `pkg/caster/out` (gone 2026-07-04) → migrate `main()` skips a missing source (lib still raises), seed conditionally mounts `/hist`. Re-ran the partition FROM_FAILURE via Dagster GraphQL; episode live on mouthpiece.iridi.cc.
- [portal-oauth 0025 gotchas](portal-oauth-0025-gotchas.md) — PROJECT 2026-07-07 **COMPLETE — all acceptance A–H met incl. the real claude.ai connect (its DCR client in the audit trail, tool calls from chats work)** — portal-server now runs a single-user **OAuth 2.1 AS** (SDK `mcpAuthRouter` + `PortalOAuthProvider`: DCR w/ claude.ai+loopback redirect allowlist, consent page keyed on `portal_mcp_api_key`, opaque sha256-hashed tokens, rotate-on-refresh, JSON state on the `artifacts/portal-oauth` bind mount — survives redeploys) because claude.ai custom connectors are OAuth-only; `/mcp` dual-auth (legacy key OR token, Claude Code untouched) + 401 `resource_metadata` discovery header. **Gotchas: SDK `registerClient` gets `client_id` pre-stamped; Express sub-app called as bare `(req,res)` from raw-node dispatch; `authorize()` may render HTML (redirect happens at `/consent`); derive the PRM URL with the SDK helper (path-suffixed `/mcp`, trailing-slash issuer); `/consent` outside SDK rate limits = accepted risk.**
- [portal 0023 gotchas](portal-0023-gotchas.md) — PROJECT 2026-07-07 **COMPLETE — all 6 slices (`87f633f`…`18cecff`) + DEPLOYED LIVE on `portal.iridi.cc` + GM-installed + LIVE ACCEPTANCE E/F/G PASSED (all A–H met)** — portal = a TS **MCP server** (10372, `astra.portal`) + a **custom astra-owned FoundryVTT module** (dials out from the GM browser) so an LLM can **search** the pf2e compendium/world + **create** (import statblocks, drop tokens, journals) against the live **pf2e "Faerrin"** world. **Acceptance gotchas: module dials only on `ready` (settings change ⇒ GM must F5); `search-compendium type` = pack `metadata.type` "Actor" NOT "npc" (fixed `e3de7bf` — zod JSDoc never crosses the MCP wire, use `.describe()`); `folder` must pre-exist.** **THE decisions held:** clone-from-compendium ONLY (D5); creates ON by default behind isGM ∧ bridge-key ∧ cap ×2 (server pre-check + module 50-ceiling) + full audit (D8); two-hop key auth; nested `apps/portal/{server,module,shared}` (pnpm `apps/portal/*` glob + uv exclude); install-by-Manifest-URL (fflate in-process zip, absolute URLs INSIDE the zip too). **Build gotchas (see file):** replace-adopt must clearInterval the heartbeat (double-ping kills healthy sockets); backoff resets only after a ≥10s healthy hold (open-reset = wrong-key hammering); the MCP-SDK `as never` seam; the 11-Dockerfile manifest ripple; oxlint OOM → `--threads=4`; routeTree.gen.ts flap; ~60s new-subdomain TLS. Scope `…/research/2026-07-06-portal-0023-thoughts.md`, spec `…/specs/0023-portal-spec.md` (BUILT).
- [Vite+ cutover 0022](viteplus-cutover-0022.md) — PROJECT 2026-07-02 **ALL 15 SLICES BUILT, R1–R6 COMPLETE (S1 `09bfc42`…S15): vite 8 lockstep + vitest everywhere + Node 24 runtime (srvx+send; THE S6 Range/206 gate sha256-PASSED on the real corpus) + pnpm sole package manager (S11) + oxlint/oxfmt gate swap, biome retired (S12–S13) + tsdown, rollup retired (S14) + vp (vite-plus 0.2.2) orchestrating CI (S15, D7 MET).** **S15 findings:** install story = `vite-plus@0.2.2` exact-pinned root devDep (npm's REAL VoidZero package; `vp` on npm is an unrelated namesquat) — `pnpm install --frozen-lockfile` is the whole CI install, no curl/new action; `vp migrate` run + reviewed + **FULLY REVERTED** (silently aliases `vite`→`@voidzero-dev/vite-plus-core` via a pnpm catalog, undermining the D9 exact-pin; its own format step crashed loading strider's config — no `nodeTsResolve.mjs` chain; rewrote 137 files incl. a committed generated file); **root `package.json` scripts sharing a task's name self-collide with `vp run -r <task>`** (raced itself, `ENOTEMPTY` crash) → removed root `typecheck`/`test`/`build` scripts, call `vp run -r <task>` directly; `vp env` doesn't exist in 0.2.2 (stale scope-doc assumption) — Node pinning is automatic via `.node-version`, proven by shadowing a decoy `node` on `$PATH`; member coverage 21/21/20-by-design (orator-controller uses `bundle` not `build`, pre-existing); local cache real for typecheck (96% warm-hit, ~23% faster full lane) but **near-useless for test** (vitest's own results-cache file trips vp's self-modified-input guard, 0% every run) — noted, not chased (CI doesn't persist cache anyway). ts-lint stays direct oxlint/oxfmt (vp's wrappers add no scoping). **DEPLOYED + LIVE-VERIFIED same session** (deploys unparked mid-session; `just up` at S9 + the S11 exit gate + final-at-HEAD after S15 — all services healthy, 8-page real-WebGL visual check, Range/206+416+HEAD both audio hosts through the edge, `/render` PNG, overlay SSE, weal-bot Discord reconnect, SigNoz three signals 0-error; human-only residue: interactive Discord roll, orator voice-join, in-player seek). **0022 CLOSED — all criteria A–N met incl. L (GHA run 28628654215 green under vp).** Spec `thoughts/astra/specs/0022-viteplus-cutover-spec.md` (D1–D14). S9–S14 gotchas now detailed in the memory file (decorators-need-tsc-before-tsdown; the 3 extra bare-bun classes; the JSX load hook; serveFile-not-bare-fs; the routeTree.gen.ts flap).
- [astra migration research](astra-migration-research.md) — the spine: re-architecting faerrin → this astra repo (Dagster+Compose/SigNoz/litellm+dspy/KDL+SOPS/uv+bun/TanStack); ledger A–I decided; **MIGRATION COMPLETE 2026-06-23 — Phases 0–6 all done (incl. 0013 vellum-frontend + the Phase-6 cutover `0015`); astra is the campaign's live stack** (every public host serves, pipeline+bots live with SigNoz traces, all data migrated, faerrin decommissioned; **faerrin's working copy DELETED from disk 2026-07-04** — "grep faerrin" no longer possible locally); leftovers are by-design/nice-to-have only; research at thoughts/shared/research/2026-06-18-astra-migration-discovery.md, plans/specs at thoughts/astra/
- [pipeline reorder 0021](pipeline-reorder-0021.md) — PROJECT 2026-06-30 **BOTH CHANGES DONE + DEPLOYED + LIVE-VERIFIED** — reorder the pipeline to craig → (transcribe ∥ merge audio) → chronicle → mouthpiece. **Change A (parallelize scribe, `6dc4a63`):** split `session_outputs` → `session_tracks → (session_audio ∥ session_transcript) → session_cleanup` fan-in; new `extract_session_tracks` (atomic persist to `cfg.tmp_path`); spans `scribe.{extract,merge,transcribe,cleanup}`; paths FROZEN, no schema change; proven via synthetic Craig fixture (merge∥transcribe overlap in SigNoz, separate traceIDs per multiprocess step); teardown deletes zip+saved+linguist data+both dynamic partitions+runs; `2026-6-27` incoming zip is degenerate (no `.aac`). **Change B (chronicle→mouthpiece context, S1 `f66f48e`/S2 `0d52198`/S3 `454d55a` + deploy):** **3→6** most-recent same-show prior episodes (TUNED `01216e1` to flat-full — synopsis + all beats + cliffhanger for EVERY episode, budget 6k→26k) + best-effort season arc → `continuity.py` block injected at the script stage (mirror `threads_block`, byte-identical prompt when empty, own `CONTINUITY_BUDGET`); linguist selectors `load_episode_summary`/`recent_prior_entries`/`season_for`; hard gate + carve-out in `linguist_output_sensor` — **THE invariant: move partition-registration to `ready` sessions only (a gate-closed session must stay un-partitioned/re-discoverable, else it never runs after its episode lands); first-eval adoption still adopts the whole backlog (paid-replay guard)**; package-path coupling (no config). Live-verified by re-rendering 2026-6-29's `session_script` (GLM ~19min, `mouthpiece.continuity_episodes=3` — **filter on the attr in SigNoz, raw trace search omits custom span attrs**; prove-and-revert). **Later TUNED wider (`01216e1`) + re-rendered 2026-6-29 to REPLACE it live** ("The Canary in the Piston Room", real ElevenLabs `mode=dialogue`; **GLM-5.2 `z-ai/glm-5.2` = 1,048,576-token context** so 6 full eps ~5.7k tokens is trivial — output `default-max-tokens 16000` is the only limit): swap `script.json`, in-container **audio-only** materialize (`--select session_audio_clips,session_episode`; **`sh -c` not `sh -lc` or PATH wipes → `dagster: not found`; `/opt/venv/bin/dagster`**) → `just mouthpiece-publish`+`-seed`+frontend redeploy → `just up` rebuilds `dagster-code` (WITH SOPS env) for future eps. **⚠️ THE timer gotcha (refined): the linguist-commit `--user` timer's `git commit` sweeps only STAGED files** (caught Change A's just-`git add`ed files → mislabeled `chore(linguist): auto-commit` commit; reset to origin not force-push) — keep a clean index across the timer window / `systemctl --user stop linguist-commit.timer` during manual git/deploy. Specs `…/specs/0021-pipeline-{scribe-parallel,chronicle-context}-spec.md` (both BUILT). Builds on [[chronicle-0019-gotchas]] + [[mouthpiece-glm-debate-switch]] + [[pipeline-live-run-gotchas]] + [[heartwood-0020-gotchas]].
- [heartwood 0020 gotchas](heartwood-0020-gotchas.md) — PROJECT 2026-07-12 **⏸ ON HOLD (stakeholder call) after the FIRST REAL APPLY: 2 human-penned creates LIVE on akasha (`Bestiary/{Auger,Ugathal}`, `fd6e47e`) — D1 create-half PROVEN; rewrite-half open (0/19 approved, 47/58 undecided; apply idempotent — approve ≥1 rewrite → re-run closes D1); THE apply gotcha: recipe staged only review.kdl, not the human-edited proposal bodies → rebase abort (FIXED `ebdef37` — stage whole `proposals/<date>`); akasha URLs case-preserved (`/Bestiary/Auger`); decision `target-path` wins over manifest path. FACTS-ONLY REWORK COMPLETE + LIVE (scope→spec→build→deploy→live-gate one day): LLM drafting RETIRED — cards show FACTS, the HUMAN writes every body in the editor** (creates open on a skeleton, rewrites on the live page verbatim; proposer = ZERO LLM calls, `draft.py`/`voice.py` deleted; `2025-8-28` regenerated 58 pages 39c/19r, the 8 LLM-only "already-known" skips resurfaced as cards by design). **⭐ THE gotchas: the debounced-save race** — flush-on-unmount + `canApprove` gates on PERSISTED save, else write→tab-switch→approve applies the empty skeleton (`apply.py` reads disk) — **+ its sibling: remount must reseed the editor from the LIVE buffer, not the body prop** (else the next keystroke saves stale text over flushed edits); SigNoz frontend service = `astra.heartwood-frontend` NOT `astra.heartwood` (looks like zero spans); a single fresh counter increment is invisible under increase/rate scalars (prove via series birth); **a subagent restarted the linguist-commit timer unprompted** (re-verify `is-active` after every agent). apply/review.kdl contract needed ZERO changes. The content acceptance is half-done (creates proven; rewrite pending resumption). **THE Phase-4 gotchas:** `user: "1000:1000"` on the compose unit is THE bind-mount write-back fix (containers run as root → writes land `0:0`, host can't commit); browser-decides/host-applies split (public surface only stages `review.kdl` to a narrow rw mount; the corpus write+commit+redeploy is the host-run recipe); the cross-language `review.kdl` contract (hand-rolled KDL writer TS↔Python, shared fixture round-trips both lanes — `@bgotink/kdl` is parse-only); apply order = validate (bun) → `uv run akasha-snapshot` → path-scoped commit+rebase → akasha redeploy; the linguist-commit timer pushed my original S5 before the amend → reset --soft + re-commit the delta (never force-push); `test_apply.py` basename collided with linguist's (no `__init__.py` → full `uv run pytest` fails); vellum editor ported verbatim behind `<ClientOnly>`; `grep -a` for SSR HTML; the `*.iridi.cc` wildcard minted the cert in ~20s.
  <br>**[superseded notes below]** Phases 1 + 2 (extraction) + 3 (prose proposer) DONE. **Phase 3 (`f9e3ce0`…`e3a57f8` 5 slices; acceptance `0dfb6e0`; hardening `9c1bbd8`/`8624ff7`):** new `proposer/` sub-package, facts→`call_text` draft→tell-lint→bounded revise→committed KDL manifest + sibling `.vellum` under `proposals/<date>/`; read-only (writes/surface = Phase 4); ~$0.10/run (GLM-5.2). **THE lesson: this feature FAILED TWICE in faerrin** ("voice may be partially unlearnable by LLMs"); Phase 2 absorbed their extraction failure; Phase 3 PORTS their assets (`DRAFT_SYSTEM`, GOOD/BAD calibration, tell-lint, page-type suppression). **THE Phase-3 verdict: CREATES pass the house-voice bar, but the full-body-replace REWRITE flattened the human's prose (3/12 lost 2nd-person POV, 9/12 shrank) → stakeholder §11 "harden rewrites" → P3.9 REVISED to PRESERVE-AND-APPEND** (keep the existing frontmatter+body verbatim, append a short passage in the page's named POV; `pov_shift` warning re-drafts if it drifts). **Lesson: don't trust an LLM to faithfully rewrite/merge into hand-authored prose — generate only the NEW material and append.** Also: P3.15 novelty gate (skip already-known rewrites), P3.17 conflict-flag, P3.10 non-prose skip, item/folder-less placement flagged; `call_text` is the prose path (page BODY lives in `apps/akasha-backend/content/<path>.vellum`, NOT the snapshot); `.kdl`/`.vellum` need no biome exclude; **`dagster/Dockerfile` was missing `COPY apps/heartwood-backend` (latent Phase-2 gap) — a new pipeline app needs the Dockerfile COPY + the root `definitions.py` assets list**. net-new multi-phase subsystem — GLM-5.2 maintains the akasha **setting wiki** (the "nouns") from transcripts via **human-gated PR-style review** at bespoke `heartwood.iridi.cc` (vellum-editor base). 5 phases (ontology infra ✅ → **extraction** ✅ → **prose proposer** ✅ (anti-slop was THE bar) → **review+write-back ← NEXT** → backfill (Phase 5)), human-gated through Phase 4. **Phase 1** (`139db9f`…`e0458f9`): `world` field + **`astra-lexicon`** lib (`defs.yaml→defs.kdl`) + **`ontology-entity`** typed registry (311, drift gate) + **`resolve()`** (Y'shael→Ichel). **Phase 2** (`a908184`…`e0508ad`): new read-only **`apps/heartwood-backend`** = world-filter (40 ingested/3 world-drop/1 EXCLUDED, NOT 41) → **filter** (drop OOC/combat/play-by-play) → **extract** noun-facts → **resolve()** → **refine (Stage 2.5)** → committed `facts/<date>.json`. **Acceptance CLOSED = GOOD PASS on the FIRST TSD session `2025-8-28`** (relocated off held-out 2026-6-8 to process the campaign in chronological order): `facts/2025-8-28.json` (`e0508ad`) = 149 facts/38 refined-out/95 dropped, page-aware (47 existing-page/72 known-no-page/25 new). **THE Phase-2 gotchas:** Dagster `@dg.asset` module must NOT `from __future__ import annotations` (dagster validator rejects the stringified context type); biome formats committed JSON → `!**/heartwood-backend/facts/**` in biome.json (scans untracked too); **`astra_llm` forced-tool calls fail two flaky ways on GLM/OpenRouter → bounded retry both (`_TOOL_JSON_ATTEMPTS=3`): malformed JSON (`98ef460`) AND `finish_reason=stop` w/ no tool call (`608fc63`)**; **GLM-5.2 reasoning tokens SHARE `max_tokens` → 8k truncated the exhaustive fact list even per chunk → raise EXTRACT/REFINE budget 8k→16k + chunk 16k→4k (`a0e13ee`); shrinking chunk alone did NOT fix it; the old "144 facts" was a SILENT truncation**; **P2.1 REVISED umbrella — PCs ARE wiki-eligible**; the feedback-driven **refinement pass** drops non-wiki facts by typed category (event/ability/possession/mechanical-gold-stats/nonsensical) + **canonicalizes resolved names so a mislabel like `Y'shael` NEVER surfaces** (kept facts AND `refined_out`; deterministic `re.sub` backstop); **page-awareness = YES at resolution** (registry seeded from the akasha snapshot; resolved+page=update / resolved+no-page=new-page / unknown=net-new) but heartwood's OWN facts feed forward only after **Phase-4 write-back** → full backfill is Phase 5; accuracy/`Voidheart→voidward` false-link = Phase-4-review territory. **▶ RESUME: Phase 4 — `heartwood.iridi.cc` review surface + corpus write-back — SCOPE it first** (Phases 1–3 read-only + done). Umbrella `…/2026-06-27-heartwood-0020-thoughts.md`, specs `…/0020-heartwood-phase{1-registry,2-extraction,3-proposer}-spec.md`. Builds on [[chronicle-0019-gotchas]] + [[config-single-source]] + [[no-ci-monitoring]].
- [chronicle 0019 gotchas](chronicle-0019-gotchas.md) — PROJECT 2026-06-27 **COMPLETE + LIVE on `akasha.iridi.cc/chronicle`**: an automatically-structured **Show → Season → Episode** campaign timeline in akasha. GLM-5.2 (`call_structured`, NOT the dspy judge) summarizes each session → Rich `EpisodeSummary` (`session_episode_summary` per-session asset) + groups each show into seasons (`campaign_timeline` aggregate, hourly schedule, skip-when-unchanged `inputs_hash`); frontend `/chronicle` index + `/chronicle/$show` (compact cards) + `/chronicle/$show/$episode` (detail) gothic views, nested in the Looking Glass Explorer. 7 slices + review follow-ups. **THE gotchas:** show = transcript filename prefix (`<NNN>.<slug>.<date>`) = the campaign-matched sessions; the mislabeled **2025-8-11** ("Argyle" false-match) is hand-excluded via `EXCLUDED_DATES` → **44 episodes** live (the 2026-6-29 orphan backfilled 2026-07-02); **any episode-set change makes `campaign_timeline` REGROUP the whole show from scratch (no prior-seasons anchor) — renumbers seasons + season-nested URLs; ACCEPTED as designed 2026-07-02, don't "fix" without a new decision**; the **linguist-commit timer auto-commits `timeline/` + redeploys mid-session** (commit code + redeploy yourself after a frontend change); season grouping must output compact **boundaries** (title/arc/start_date) not episode-lists (33-ep show truncated mid-JSON) → `_seasons_from_plan` forces a total ordered partition; non-zero-padded dates need `date_key` int-tuple sort; backfill on the HOST (SOPS resolves), resumable, ~$2–3; biome formats JSON → exclude `linguist/timeline/**` + repo-wide pre-commit gate; `routeTree.gen.ts` is COMMITTED (regen via `bun run build`); deploy = linguist-commit timer + akasha Dockerfile must add `apps/linguist/timeline` COPY. Builds on [[akasha-frontend-0011-gotchas]] + [[linguist-gate-j-dspy-judge]] + [[config-single-source]].
- [harrow 0017 gotchas](harrow-0017-gotchas.md) — PROJECT 2026-06-26 **COMPLETE + LIVE on `harrow.iridi.cc`**: ported the external tarot reader (`/ruby/data/experiments/tarot`, "Harrow") into astra as a **backend-less SSR frontend on the strider template** (port 10369, `astra.harrow`), 6 CI-green slices + an edge cutover (host takeover — DNS pre-existed; removed the old parent-Caddyfile stanza, validate-merged-edge + reload; old container still unrouted on :10204). **THE gotchas:** the three SSR/hydration seams (client-only `Math.random` draw behind `<ClientOnly>`, `matchMedia`→shell `useIsMobile`, reveal timers); astra's `noUncheckedIndexedAccess` breaks verbatim ports (index/match-group/swap assertions, same logic); full **gothic re-skin** = remap harrow tokens→gothic + deck hues via gothic **identityStyle** seam (`--identity-color`, Decision G); clickable card→native `<button>` (biome `noStaticElementInteractions`); parity gates via a source-captured deck golden + the **29 verbatim predicate labels** (*Devil/Godhome/Slip/Mortal Rising*); coupled cards+spreads content sources w/ RELATIVE-path parser imports; `/spreads/history` via flat-file routing (no layout); deploy = simplest frontend + the new-member Dockerfile manifest ripple (8 siblings); **`grep -a` to verify SSR HTML** (UTF-8 glyphs read as binary). DNS+caddy-reload deferred. **Post-0017 (`5f3865f`): added an animated yellow/black STARFIELD pixi-shader background** — ported strider's balatro page-bg idiom (Filter on a full-screen rect, `uTime` ticker, `vTextureCoord`-not-`gl_FragCoord` DPR fix) but self-contained (no PixiContext; harrow has no on-canvas content), SSR-safe via dynamic-import + `<ClientOnly>`, `pixi.js@^8.18.1`, deployed live. Builds on [[migration-guide]] + [[strider-tithe-pixi-gotchas]].
- [backdrop signature style](backdrop-signature-style.md) — PROJECT 2026-06-26 **COMPLETE + LIVE**: made the animated abstract page background an **astra signature style** — new shared lib **`@astra/backdrop`** (`ShaderBackground` SSR-safe mounter — renders null until mounted, dynamic-imports pixi, ONE Application per page — + `createShaderBackground` factory + a catalog: starfield/mouthpieceResonance/ledgerAurora, gothic-palette RGB + noise GLSL in `shaders/common.ts`, per-shader `uIntensity` knob). harrow migrated to it (dogfood); **strider left alone** (balatro entangled w/ faction-tint). **THE constraint: two pixi Applications on one page CONFLICT** → mouthpiece/ledger/harrow get pixi shaders; **akasha (already runs a webgpu force-graph) gets a CSS-only nebula instead** (no 2nd context). **Verify bg visually in a real WebGL browser** (Playwright swiftshader, ~5s wait) — SSR HTML can't show it; first pass too faint → tuned. New TS lib needs NO Dockerfile ripple (frontends COPY libs/ts wholesale). Builds on [[harrow-0017-gotchas]] + [[strider-tithe-pixi-gotchas]].
- [ledger 0018 gotchas](ledger-0018-gotchas.md) — PROJECT 2026-06-26 **COMPLETE + LIVE on `ledger.iridi.cc`**: built **ledger**, the astra **landing page** — a backend-less SSR frontend (strider template, port 10370, the simplest in the repo, sibling of harrow) with a gothic card grid linking the 5 player-facing sites. **THE pattern: config-single-source for a cross-site link registry** — build-content reads each linked site's own `public-origin` from config.kdl + joins a ledger-owned registry (title/blurb/order) → generated `sites.ts`, **zero hardcoded URLs** (added a `public-origin` to strider to complete it); `sites.test.ts` gate. 3 CI-green slices (`e6aeaea`/`cab6ebe`/`ae8b27e`). Deploy: the 9-sibling Dockerfile manifest ripple + uv exclude + backend-less targeted `up`. **THE edge surprise: the new `ledger.iridi.cc` subdomain JUST WORKED with no manual DNS record** — `*.iridi.cc` is a wildcard, so it resolved immediately + Caddy ACME-DNS minted a real Let's Encrypt cert (prior frontends needlessly *deferred* DNS; try the wildcard first). Builds on [[harrow-0017-gotchas]] + [[migration-guide]] + [[config-single-source]].
- [gothic frontend design polish](gothic-frontend-design-polish.md) — PROJECT: 2026-06-24 design pass over gothic + the 4 frontends (live-captured + screenshot-verified, 6 CI-green slices). **THE gotcha: an unlayered `*{margin:0;padding:0}` reset outranks gothic's `@layer components` and silently zeroes ALL rendered-vellum padding/margins** (pills, code blocks, prose spacing) — wrap the reset in `@layer base` (fixed in akasha/strider/vellum-frontend; documented in `apps/strider/README.md`). Also: gothic now styles `pre`/`code`/`blockquote` + lifted card contrast + padded pills (**regenerate the vellum-render VR goldens in the pinned container after any gothic change**); akasha reading-measure cap + 404 + mobile-first + tag/graph/search polish; strider faction dossier headings; orator centered sign-in; Tormeré transcript content fix. **Follow-ons same session: a new `:::deity` vellum construct (DeityCard, rendered run-in not a grid) + the WHOLE akasha corpus authored in the VSS brace surface — closed VSS gaps (optional block title + `@fields`/`@timeline`), zero `:::` openers remain; `.gitattributes` maps `*.vellum`→Markdown on GitHub.** Audit at thoughts/shared/research/2026-06-24-gothic-frontend-design-audit-thoughts.md
- [mouthpiece two-host gotchas](mouthpiece-two-host-gotchas.md) — PROJECT 2026-06-24: the recap podcast went **3→2 hosts (Pip rolled into Maeve)** + calmer prompts (`c2309df`, snapshot `bad00bb`). **THE back-compat mechanism: per-episode hosts** — `episodes_index._read_hosts` + `SessionInput.hosts` + `build_index(s.hosts or hosts)`, `HostConfig.c`/`VoiceConfig.c` now optional, so the 8 published episodes stay 3-host (Pip) while new ones are 2-host (`_episode_hosts()` is just the fallback). being.kdl dropped pip + rewrote maeve → regen `being.canonical.json`; faerrin prompt-fidelity tests deleted (deliberate divergence). **Two deploy traps:** the dagster container runs IMAGE-baked code so a mouthpiece-backend change needs `docker compose build dagster-code` before it's live; and **recreating a container with plain `docker compose up -d <svc>` (not `just up`) drops the SOPS env → silent MOCK-TTS fallback** (mode=turns, ~27s, 60s placeholder durations). Re-render one episode w/o re-running Stage 3: place script.json + `dagster asset materialize --select session_audio_clips,session_episode --partition <date>`. 2026-6-22 re-rendered live as two-host "The Sandwich Yoink Bonus"
- [mouthpiece GLM 5.2 + debate switch](mouthpiece-glm-debate-switch.md) — PROJECT 2026-06-26 **DEPLOYED LIVE** (`87d10dc`…`3d8b768`): recap podcast moved **off Anthropic → GLM 5.2** (open-weight MoE, OpenRouter) for digest/script/mega + format recap→**DEBATE** (Anthropic dropped Fable on a US-gov restriction; GLM cheaper-than-Haiku, near-Opus; stakeholders wanted debate). Model is config-single-source (`llm.default-model`, both schemas + `astra_llm.DEFAULT_MODEL`); **litellm reads provider keys from ENV** → key encrypted into SOPS + `OPENROUTER_API_KEY` added to the `*dagster-env` anchor. Debate = rewrite **Pass A only** (`build_improv_system_prompt`; Pass B faithfully typesets); forward-only (published episodes keep their scripts). scribe stays Groq Whisper (ASR); **linguist judges LATER moved to GLM too** ([[linguist-gate-j-dspy-judge]]). **THE CI gotcha: the substrate smoke shells to `sops` (absent in CI) when the key env-override is wrong → red despite green-local because I HAVE sops; mask `sops` off PATH to reproduce.** **First real debate episode rendered LIVE 2026-6-22 (`fd48ea4`)** — re-render an existing session by materializing IN the dagster-code container (host can't write root-owned `episodes/`); watch snapshot-vs-render title drift. **Longer episodes (`867eee7`+`4e0000f`): Pass B (structured typeset) is the scaling bottleneck not Pass A → CHUNK Pass B (`_split_transcript`/`PASS_B_CHUNK_WORDS`); added `REQUEST_TIMEOUT_S=300` (a hang was 46 min, no client timeout); 2026-6-22 re-rendered to 34.1 min "Rust, Numerology, and the Sea Shanty Below". Gotchas: root-owned orphan run → `docker compose restart dagster-code`; the auto-publish timer races a manual re-render.** Builds on [[mouthpiece-two-host-gotchas]].
- [akasha session audio dependency](akasha-session-audio-dependency.md) — PROJECT 2026-06-24 **DONE + LIVE**: akasha transcript audio is now served **same-origin by astra** (`akasha.iridi.cc/audio/<date>.mp3`, mouthpiece `/audio` D2 pattern — `staticMounts` + `akasha-audio` volume + `just akasha-seed`, wired into the linguist-commit timer; build-time normalize so the 78 committed transcripts need no re-gen). **The WHOLE faerrin caddyfile import was removed** from the shared proxy (all 5 blocks verified dead/replaced first) — faerrin fully decommissioned at the edge. **THE gotcha:** faerrin mislocated 4 recent sessions under a nested `wretch/data/saved/saved/<date>/` (why faerrin's own static-audio 404'd 2026-6-8) → the seed scans audio.mp3 at any depth; astra serving 2026-6-8 fixes a faerrin-broken gap. 85 sessions/~14.4 GB seeded; live-verified through the public edge (206 Range)
- [astra alerting setup](astra-alerting-setup.md) — PROJECT 2026-06-30 **DONE + LIVE + verified**; **2026-07-08 the 610-page Class C flood + debounce fix (`22ad895`); watchdog now AUTO-REMEDIATES a wedged Drive FUSE mount; 2026-07-13 the VANISHED-mount blind spot fixed (mount gone from mount table + daemon alive = all signals green, ls on the bare dir passes + craig-sync nullglobs "0 new" — a session recording sat invisible in Drive for hours; `check_mount` now requires a fuse mount in mountinfo before the responsiveness probe; unmounted = page-only, restart gdrive.service needs root)** (never stat it — mountinfo `0:<minor>` → `echo 1 > /sys/fs/fuse/connections/<minor>/abort` frees D-state waiters + `fusermount -uz` + system `gdrive.service` Restart=always remounts — the "manual mount/no unit" note is STALE; 1h debounce, `WATCHDOG_REMEDIATE=0` kill-switch, 🟠 one-notice page) — stack-wide Discord alerting (prompted by the craig-sync wedge stalling the pipeline ~6h with ZERO alerts). Three failure classes shipped (`95735d4`): **Class A** SigNoz `discord-ops` slack channel (`<webhook>/slack`) + `astra error/fatal logs` threshold rule (`severity_text IN [ERROR,FATAL]`, groupBy service.name) — lives in SigNoz's DB, not git; **Class C** `deploy/systemd/alert-notify.sh` (decrypts webhook from SOPS, curls Discord direct — survives SigNoz down) + `astra-alert@.service` `OnFailure=` on craig-sync + linguist-commit; **Class B** `astra-watchdog.{service,timer}` (15m, bounded mount probe + timers-armed, debounced). Install: **`just alert-install`**; `ALERT_DRY_RUN=1` to test without paging. **THE gotchas:** `OnFailure=` is a `[Unit]` key not `[Service]` (silently ignored); can't fail craig-sync via a missing `CRAIG_DROP_DIR` (nullglob → exit 0) so test OnFailure with `systemd-run … /bin/false`; `local a=$1 b=$DIR/$a` is an unbound-var trap under `set -u`; Discord is Slack-compatible (`+/slack` for SigNoz, native JSON for curl). **2026-07-08 flood:** the gdfuse DAEMON died (manual mount, no fstab — remount by hand, doesn't survive reboot); undebounced Class C re-paged every timer tick for 38h (craig-sync 458 + watchdog-self-timeout-from-leaked-D-state-probes 152); fix = per-unit `failure_gate()` (page, suppress ≤`FAILURE_DEBOUNCE_S`=1h, re-page w/ count, watchdog clears state on recovery); script-only edits need NO alert-install (units ExecStart the in-repo path); negative-path container verification must NOT attach to `signoz-net` (a 0027 ad-hoc run paged Class A with test-fixture ERRORs). **Webhook transited chat again → may want rotating.** Builds on [[pipeline-live-run-gotchas]] + [[deploy-sops-injection]] + [[signoz-mcp]].
- [telemetry coverage pass](telemetry-coverage-pass.md) — PROJECT 2026-06-30 **DONE + DEPLOYED + verified live** — repo-wide "add spans/logs/metrics across all services" once-over (3-agent audit cross-checked vs live SigNoz → 5 phases + a fix, `a7a6a25`…`88e6057`, `just up`'d + spot-checked). **⭐ THE load-bearing gotcha: TS metric instruments grabbed at module scope before `initTelemetry` are PERMANENT no-ops** — JS metrics has no deferred proxy (traces=ProxyTracer + logs=ProxyLogger DO defer; metrics don't), and ES import hoisting runs them before the entry's init → **silently zeroed every TS metric for the repo's life**. Fix = **`lazyCounter`/`lazyHistogram` in `@astra/observe`** (defer to first use; ALWAYS use these, never module-scope `getMeter().createCounter()`). Also: short-lived procs must `shutdown()` or buffered exports drop (5 py scripts via try/finally inside `main()` for console_scripts + 3 TS services on SIGTERM); **heartwood-frontend de-collided `astra.heartwood`→`astra.heartwood-frontend`**; mouthpiece asset spans unblocked the `astra.llm.*` cost attrs (were on a no-op span); orator-controller (desktop plugin) → public OTLP `otel.iridi.cc` via a hand-rolled best-effort emitter. Verified: `astra.orator.api.requests{unauthenticated}=5` exact match. Builds on [[telemetry-built-in]] + [[config-single-source]] + [[signoz-mcp]] + [[deploy-apply-with-just]].
- [pipeline live-run gotchas](pipeline-live-run-gotchas.md) — running the live craig→scribe→linguist→mouthpiece Dagster pipeline: the **cascade-sensor backlog ADOPTION** pattern (re-enable sensors without reprocessing the 42 seed transcripts — cursor-keyed, persists across redeploys), scribe's Groq Whisper constraints (skip degenerate sub-min/N/A cuts + probe the actual flac; **25 MB direct-upload cap** → force `-sample_fmt s16` + `max_chunk_sec=480`), transient-provider resilience (litellm `num_retries` + Dagster `RetryPolicy` on the external-API mouthpiece steps), how to re-execute a failed partition (`launchRunReexecution` FROM_FAILURE); the **full end-to-end run VERIFIED** (both real sessions 2026-6-18/22 produced complete episode.mp3+transcript, 42 seed untouched); the ElevenLabs gotcha (a TTS 401 was out-of-credits, NOT a bad key — don't validate the scoped key via `/v1/user`, it lacks `user_read`); config.kdl now authoritative for the scribe/mouthpiece models (`93641e9`); **akasha-frontend auto-redeploys via the linguist-commit timer + cutover gates loosened for live growth (`079c045`/`ed3c561`)**; and the **mouthpiece-frontend live-pipeline integration — DONE+LIVE-VERIFIED (`a472d54`…`7c25d2f`): `discover_sessions` keys on the script id not the date-dir, faerrin's back-catalog migrated (`just mouthpiece-migrate-history`, live-precedence dedup), audio seed gathers historical+live, the committed snapshot is live-derived (9 eps, content-agnostic gates), and the linguist-commit timer auto-publishes+redeploys on new episodes**
- [vellum-frontend 0013 gotchas](vellum-frontend-0013-gotchas.md) — 0013 (faerrin `vellum` → the document-forge **editor** + a **PNG render service**) **COMPLETE — all 7 slices built + pushed + deployed-local + verified live**; the **final** 0011–0013 frontend. **Two Compose units (D2):** vellum-frontend (SSR editor, **10367**) + vellum-render (Bun+Playwright, **10368**), same-origin `/render` via Caddy (`/render`+`/health`→10368, `/editor` local_only + rest→10367) + a dev Vite proxy. Editor is an `ssr:false` route, faerrin `src/app/`→`src/domain/editor/` ~verbatim (gothic `--color-*` var remap, one CSS module); **D5 dropped the ⇄ Syntax button** (no canonicalToVss, VSS authoring still intact via parseDocument); full-vellum `:::fields`/`:::timeline`/`[[crossref]]` added (canonical-only — no VSS sugar) + the R2 SIGIL-sync gate. **vellum-render = the FIRST browser-in-a-container** (bun-slim + `playwright install --with-deps chromium`, Chromium pinned by the lock so it matches the CI VR container; egress block needs same-origin fonts via gothicFontsPlugin; render-entry is a SEPARATE plain-Vite build). **VR gate (D4):** goldens regenerated against astra-gothic in the pinned `oven/bun:1.3.14` container (new-baseline, not faerrin parity), a `ci.yml` job runs the compare in that same container; 7 fixtures, 0.000% drift. Verified: containerized Chromium renders a real PNG, SigNoz spans for both services. **Now LIVE on vellum.iridi.cc** (DNS set + caddy-reload). See the file for the deploy ripple + the faerrin `vellum.iridi.cc` decommission.
- [mouthpiece-frontend 0012 gotchas](mouthpiece-frontend-0012-gotchas.md) — 0012 (faerrin `face` → SSR podcast player, port 10366) **COMPLETE — all 6 slices built + pushed + deployed-local + verified live** (healthy on 10366, audio Range-serves, SigNoz SSR spans); the third 0011–0013 frontend + the simplest worked example (one committed snapshot → generated modules → routes, no editor/graph/search). s1 `episodes_index` BACKEND asset (D1, backend owns ALL shaping incl. the inlined transcript) + committed `snapshot/episodes-index.json` (akasha-snapshot pattern + freshness gate); **REVISED D6: transcript inlined → frontend ports NO helpers**; routes read static generated modules directly (not `useLoaderData` under `@ts-nocheck` tree); dotted `$id` lossless; gothic re-skin (plain CSS after gothic's theme.css, no double tailwind import; 3 fixed host colors); `Player` 1:1 Solid→React (live-ref fix for the stale-closure trap, SSR-renders no ClientOnly); deploy via `createSsrServer` `staticMounts` (Range-serving `/audio/` off a runtime volume — 173 MB out of the image) + `just mouthpiece-seed`; durationMs=0 in the snapshot (Player's loadedmetadata authoritative, D5); now LIVE on mouthpiece.iridi.cc
- [akasha-frontend 0011 gotchas](akasha-frontend-0011-gotchas.md) — akasha-frontend (the wiki read-surface, SSR strider-template port of faerrin aether) **COMPLETE — all 9 slices built + deployed-local-verified** (scaffold, slug/site lift+parity gate GREEN, routes+static emits, vellum render+crossref, islands+page shell, client-only pixi/d3 graph, transcripts D4/N7 — 76 Script pages EXACT 1:1 slug parity + bodies code-split behind a createServerFn, Pagefind search N1 — NodeJS Indexing API over in-memory HTML 217 pages, URL-parity cutover gate GREEN 217==faerrin + deploy on 10365 w/ SigNoz SSR spans); now LIVE on akasha.iridi.cc; the second 0011–0013 frontend + a worked example for 0012/0013; reframings (SSR not prerender, gothic renders vellum so no remark chain, transcripts are a SEPARATE artifact merged into the graph); seams de-risked (N1 Pagefind NodeJS API, N3 gothic resolveCrossref); locked N4/N6/N7 (committer date, consume snapshot edges, port matchCampaign); verbatim slug.ts/site.ts + TranscriptPlayer; slice-3: catch-all `$` route (content/folder/alias), `body[data-slug]`, alias meta-refresh via React 19 head hoisting, static emits to public/→dist/client, node-free site.ts, public-origin config; slice-4: build-time gothic DocumentView render baked to generated/bodies.ts + injected (dangerouslySetInnerHTML), N3 crossref resolver from snapshot edges, **gothic theme.css needs `@source "./"`** (Tailwind v4 skips node_modules → DocumentView consumers ship gothic utilities unstyled — template fix for all frontend ports); slice-5: 4 React islands (Darkmode dark-only+FOUC head script, ReaderMode, Popover@floating-ui on a[data-crossref]/a.internal, Explorer SSR-safe localStorage collapse) + full 3-col page shell + functional gothic CSS, N5 useEffect teardown; speaker-color name→value via ontology-being; port 10365
- [weal 0009 gotchas](weal-0009-gotchas.md) — building weal-bot + weal-overlay: roller parity gate (deterministic surfaces + serde round-trip on real funcs payloads), host flavor banks now in ontology-being weal-host (GSR-only), migration reality (~8.9k clean rows not 47M; SQLite→PG migration DONE 2026-06-23), RNG clone-quirk not ported, overlay = Bun.serve SPA + gothic v4, deploy units; bot LIVE on a real SOPS token + roll history migrated
- [orator 0010 gotchas](orator-0010-gotchas.md) — orator-backend (lark lift) + orator-controller, BUILT + deployed-local-verified: sync-bun:sqlite→async-Bun-SQL(Postgres) ripple incl. the live-PG `= any($1)` malformed-array bug (Bun SQL.unsafe stringifies array params → use `in (…)`), no-live-PG test doubles, voice/DAVE = prebuilt napi (no compile), M3 Router client SPA in src/web (public /rum-config, gothicFontsPlugin), M4 birdfeed configurable origin, config-single-source scrub (kdl holds deploy values; mirror both schemas), deploy (Dockerfile copies ALL app manifests for frozen lock; ffmpeg+yt-dlp; M2 migration RAN+verified 87 tracks); now LIVE on orator.iridi.cc (Discord bot live; physical Stream Deck hardware test n/a)
- [deploy artifacts + run-as-user](deploy-artifacts-run-as-user.md) — PROJECT 2026-06-30 DONE+LIVE+pushed (`6cd6c45`): the 6 astra named volumes (audio+Postgres) → bind mounts under a gitignored `/artifacts/`, and all 13 app/Dagster containers run as `user:"1000:1000"` so pipeline writes land host-owned (the heartwood precedent, stack-wide). SigNoz kept its named volumes. **Gotchas:** container paths unchanged → NO config.kdl edit; `oven/bun` ships `bun`=uid1000 (frontends just work); **Dagster needs `chown -R 1000:1000 /opt/dagster` + writable HOME** (compute logs/storage) else EACCES; vellum-render needs `chown /ms-playwright`; **Postgres bind keeps uid 70 / 0700, NO `user:`** (`pg-migrate` copies + chown 70); **docker-as-root replaces host sudo** (sudo needs a password here); a missing bind source is auto-created by Docker as ROOT → `just artifacts-init` (mkdir as 1000) from `up`; seed recipes re-pointed; bonus `linguist/timeline` mount fix. Old volumes kept as ~25G backup. Builds on [[deploy-sops-injection]] + [[deploy-apply-with-just]] + [[heartwood-0020-gotchas]].
- [deploy SOPS injection](deploy-sops-injection.md) — containers never decrypt SOPS themselves (no sops/age/secrets in images); `just up` decrypts on the host + injects each secret as an UPPER_CASED env var, and config's env-override path resolves it in-container. Built 2026-06-21 — before it, EVERY secret-needing deployment was broken (weal-bot crash-looped, orator ran degraded, pipeline LLM calls would fail). weal now live (real token). Add a service's keys to its compose `environment:` when it gains a `ref=`
- [SigNoz MCP](signoz-mcp.md) — prefer the `signoz_*` MCP tools for ALL astra observability queries (verified working vs the live SigNoz; not curl/clickhouse)
- [no CI monitoring](no-ci-monitoring.md) — after pushing astra to origin/main, don't watch the GHA run to completion; reproduce CI lanes locally first, confirm push + one status check
- [config single-source](config-single-source.md) — ALL config lives in ontology/ontology-config + read via astra_config (py+ts); no hardcoded/duplicated config or ad-hoc env reads outside astra_config
- [telemetry built-in](telemetry-built-in.md) — every app/asset must wire traces+metrics+logs to SigNoz in its actual runtime (call init_telemetry in the Dagster code location / service entrypoint); importing observe ≠ wiring it
- [linguist gate J — dspy judge compile](linguist-gate-j-dspy-judge.md) — how the optimizable judge + MIPROv2 compile works (gold set, miner, TUI, optimize.py --live); committed judge.compiled.json. **RETUNED onto GLM 5.2 2026-06-26** (`ensure_openrouter_env`, escalation now inert since judge==escalate, recompiled artifact P0.936/R0.779; DEPLOYED LIVE via `just up` 2026-06-26)
- [flag paid/live actions](flag-paid-live-actions.md) — FEEDBACK: paid or live-content-replacing steps (TTS credits, snapshot publish, episode replacement) get an explicit one-line flag at the point of execution, even when a spec sanctions them — spec-sanctioned ≠ moment-sanctioned
- [verify before acting](verify-before-acting.md) — FEEDBACK: check what exists (prior astra art, skill docs, repo conventions) before acting; mirror the nearest already-built subsystem, don't reinvent or assume
- [no silent scope cuts](no-silent-scope-cuts.md) — FEEDBACK: never shrink/defer spec'd scope to fit my budget; surface the trade-off and let the user decide; only defer what the spec sanctions
- [resolve open questions before next stage](resolve-open-questions-before-next-stage.md) — FEEDBACK: never advance a dev stage (scope→spec→implement) with open questions in the doc; ask them as they arise while writing (AskUserQuestion, up to 4 related decisions per call — batch, don't drip), record each RESOLVED, only then proceed
- [deploy: apply with just](deploy-apply-with-just.md) — FEEDBACK: after a deploy/edge change (compose/Dockerfile/server.ts/config.kdl/sites.caddyfile), `just up` (rebuild+recreate) + `just caddy-reload` + verify — local edits aren't live until then
- [tanstack start skill](tanstack-start-skill.md) — REFERENCE: working on a frontend? read the bundled `@tanstack/react-start/skills/.../SKILL.md` first; server-side = `createServerFn` (no file server-routes in the pinned 1.168), don't reach for middleware
- [strider editor auth accepted](strider-editor-auth-accepted.md) — PROJECT: the strider editor write server-fn lacks auth by design (personal low-traffic site; writeLayer.ts already blocks traversal/overwrite) — don't re-flag it; caveats = public/multi-tenant deploy or ever rendering layer body as HTML. Full template review at thoughts/shared/research/2026-06-21-strider-template-review-thoughts.md
- [strider 0016 gotchas](strider-0016-gotchas.md) — PROJECT: template-hardening spec 0016 **COMPLETE** — all 7 slices pushed+live-verified (idiom/tests/dedup/perf/observe/@astra/content-build + @astra/site-kit + shell-vs-src/domain split + port README). Load-bearing for the 0011–0013 copies: **vite `--configLoader runner`** to import a workspace TS pkg from vite.config; createServerFn stays in app; build-stage needs config.kdl COPY; fonts self-serve from the container; **biome path-override globs** must follow a file move; a new TS lib needs ≥1 test (else `bun test` exit-1 reds CI); flip-anim contract + coupled content sources + no canvas test. Telemetry: OTLP must be the in-cluster `signoz-otel-collector:4318`, not localhost
- [migration guide](migration-guide.md) — REFERENCE: read `thoughts/shared/guides/migrating-an-app-into-astra.md` (sequenced playbook + ecosystem dos/don'ts + framework cheat-sheet) **before** porting any non-TanStack app, esp. frontends 0011–0013; pairs with `apps/strider/README.md`
- [strider tithe pixi gotchas](strider-tithe-pixi-gotchas.md) — PROJECT: the strider `tithe` transient layer change (a flipping purple-shader wave that fills center→edge then fades, no persistent state; `{op:tithe}` ignored by every fold) + **load-bearing pixi v8 gotchas**: filters+masks DON'T compose; rendering to a RenderTexture inside the ticker yields a BLANK target. THE recipe for a **live, continuous, animated shader clipped to flipping tiles** = gate the shader by input alpha (`* texture(uTexture,vTextureCoord).a`) + apply the filter to a **container** of plain white `scale.y`-flipping hex tiles (NO mask, NO RenderTexture) + advance uTime each frame + an invisible full-grid bounds anchor + attach the filter only while playing. headless RAF throttling makes fast-pixi-animation screenshots miss frames (slow to ~4–5s + `reducedMotion:'no-preference'` to verify). balatro palette is now uniform-driven
- [strider factions are vellum](strider-factions-vellum.md) — PROJECT: strider `content/factions/*` are now **vellum** (`.vellum`), one document per faction, rendered build-only via gothic `DocumentView` (no-op crossref); members-split/cards gone (personnel = in-document headings); `@astra/vellum-lang` build-time devDep; plain prose renders in gothic mechanical-mode (teal headings); NOT byte-identical (verify visually); layers=KDL, factions=vellum by design
- [strider layers are KDL](strider-layers-kdl.md) — PROJECT: strider `content/layers/*` are now **KDL** not markdown (flat schema: op = node name, `slug` positional, `hex q r`/`member` children, `faction=#null`); parse/serialize seam keeps `parseChange`+folds unchanged (regenerated layers.ts byte-identical = the gate); `@bgotink/kdl` is a build-time devDep; don't import `scripts/` from a `src/` test; factions stay markdown (→ vellum in a future change)
- [strider banner alliance gotchas](strider-banner-alliance-gotchas.md) — PROJECT: the strider "factions ally under one banner" layer change (COMPLETE + LIVE, 2026-06-23) — new `banner-form`/`banner-dissolve` Change ops + `foldBanners` (dissolve reverts for free). **THE pattern:** banners render as synthetic **pseudo-factions** appended to the faction list, so all the pixi fill/hover/click/border-dissolve/flip machinery applies UNTOUCHED (HexMap needed zero changes); copy this for any "merge N entities into one painted bloc" feature. `CURRENT_FACTION_HEXES` stays pre-banner (editor-only); banners emitted as `CURRENT_BANNERS`. Net-new feature, NOT in faerrin
- [strider balatro + timeline gotchas](strider-balatro-timeline-gotchas.md) — PROJECT 2026-06-24 (COMPLETE + LIVE, `82aa7e2`/`de13a02`/`e052694`): per-faction **balatro hex tint** (ONE `tintFromTexture` filter over `factionHexLayer`, palette derived per-pixel from each hex's colour — NOT per-hex) + **THE filter-resolution gotcha** (the offscreen round-trip drops thin **horizontal** flat-top grid strokes at some zooms → `filter.resolution = renderer.resolution` + `antialias:"on"`; grid kept via deep-shadow stop + unfiltered `factionBorderLayer`). Plus the **timeline UX overhaul**: current-first default + **bounded play-once** catch-up (localStorage `strider:vox-log-seen`, cap `MAX_PLAYBACK_LAYERS=10`), constant `LAYER_DWELL_MS` (acceleration removed), `SKIP`/`REPLAY` controls + unlocked arrows, a **scrubber** (range slider, dots deleted), and **precomputed fold snapshots** (`timelineFrames.ts` `buildTimelineFrames` → O(1) `frames[layerIndex]` lookup, was O(n²)). Builds on [[strider-tithe-pixi-gotchas]]
