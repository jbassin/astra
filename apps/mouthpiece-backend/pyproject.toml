[project]
name = "astra-mouthpiece-backend"
version = "0.0.0"
description = "mouthpiece-backend — session transcript → tavern-tone roundtable script → episode audio (caster rewrite, 0008), as a Dagster per-session asset graph. LLM via libs/py/llm (raw, no dspy); grounding reads akasha; hosts/voices from ontology-being."
requires-python = ">=3.12"
dependencies = [
    "astra-akasha-backend",
    "astra-config",
    "astra-linguist",
    "astra-llm",
    "astra-observe",
    "astra-ontology",
    "astra-ontology-being",
    "astra-ontology-config",
    "dagster",
    "httpx",
    "pydantic>=2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/astra_mouthpiece"]

[tool.uv.sources]
astra-akasha-backend = { workspace = true }
astra-config = { workspace = true }
astra-linguist = { workspace = true }
astra-llm = { workspace = true }
astra-observe = { workspace = true }
astra-ontology = { workspace = true }
astra-ontology-being = { workspace = true }
astra-ontology-config = { workspace = true }
