// @astra/portal-server — MCP+WS server for the live FoundryVTT "Faerrin" world
// (spec 0023). The bridge/MCP skeleton (telemetry, WS auth, Streamable-HTTP MCP)
// lands in S2; S1 only wires the workspace member + its wire constants.
export { BRIDGE_WS_PATH, MCP_HTTP_PATH, SERVICE_NAME } from "./constants";
