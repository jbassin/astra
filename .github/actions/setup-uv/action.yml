name: setup-uv
description: Install a pinned uv and sync the workspace from the frozen lockfile.
runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v5
      with:
        version: "0.11.19"
        enable-cache: true
    - run: uv sync --frozen
      shell: bash
