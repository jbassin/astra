{
  "name": "@astra/weal-bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo '@astra/weal-bot: no build step (bun runs TS directly)'"
  },
  "dependencies": {
    "@astra/config": "workspace:*",
    "@astra/observe": "workspace:*",
    "@astra/ontology": "workspace:*",
    "@opentelemetry/api": "^1.9.1",
    "discord.js": "^14"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
