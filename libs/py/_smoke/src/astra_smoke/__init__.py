"""Phase 0 smoke library — proves the Python (uv) lane builds, imports, and tests."""


def ping() -> str:
    """Return a constant so a trivial test can prove the lane is green."""
    return "pong"
