[project]
name = "astra-linguist"
version = "0.0.0"
description = "linguist — transcript processing (0006): scribe script.json → formatted/canonical transcript + mouthpiece context, with a dspy LLM correction surfacer. Python Dagster assets. The largest pipeline subsystem."
requires-python = ">=3.12"
dependencies = [
    "astra-config",
    "astra-llm",
    "astra-observe",
    "astra-ontology",
    "astra-ontology-being",
    "dagster",
    "pydantic>=2",
    "pyyaml>=6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/astra_linguist"]

[tool.hatch.build.targets.wheel.force-include]
"src/astra_linguist/defs.yaml" = "astra_linguist/defs.yaml"

[tool.uv.sources]
astra-config = { workspace = true }
astra-llm = { workspace = true }
astra-observe = { workspace = true }
astra-ontology = { workspace = true }
astra-ontology-being = { workspace = true }
